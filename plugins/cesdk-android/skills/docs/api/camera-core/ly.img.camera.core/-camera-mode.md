# CameraMode

- **Module:** `ly.img:camera-core`
- **Package:** `ly.img.camera.core`

Represents the different modes the camera can operate in.

```kotlin
interface CameraMode : Parcelable
```


## Members

### describeContents

```kotlin
open override fun describeContents(): Int
```

### writeToParcel

```kotlin
abstract override fun writeToParcel(dest: Parcel, flags: Int)
```
