# IfNeeded

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.component.data`

Applies the preset only if needed. When applied the crop UI is opened.

```kotlin
data class IfNeeded(val threshold: Float = 1.0E-4f) : ForceCropMode
```


## Members

### IfNeeded

```kotlin
constructor(threshold: Float = 1.0E-4f)
```

### threshold

```kotlin
val threshold: Float = 1.0E-4f
```
