# SetViewMode

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.event`

An event for setting the view mode of the editor to a new value.

```kotlin
class SetViewMode(val viewMode: EditorViewMode) : EditorEvent
```


## Members

### SetViewMode

```kotlin
constructor(viewMode: EditorViewMode)
```

### viewMode

```kotlin
val viewMode: EditorViewMode
```
