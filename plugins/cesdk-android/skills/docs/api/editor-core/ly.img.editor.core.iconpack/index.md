# ly.img.editor.core.iconpack

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.iconpack`
- **Module catalog:** [`ly.img:editor-core`](<../../indexes/editor-core.md>)

## Top-level declarations

### AddAudio

```kotlin
val IconPack.AddAudio: ImageVector
```

### AddCameraBackground

```kotlin
val IconPack.AddCameraBackground: ImageVector
```

### AddCameraForeground

```kotlin
val IconPack.AddCameraForeground: ImageVector
```

### AddGalleryBackground

```kotlin
val IconPack.AddGalleryBackground: ImageVector
```

### AddGalleryForeground

```kotlin
val IconPack.AddGalleryForeground: ImageVector
```

### AddImageForeground

```kotlin
val IconPack.AddImageForeground: ImageVector
```

### AddOverlay

```kotlin
val IconPack.AddOverlay: ImageVector
```

### AddShape

```kotlin
val IconPack.AddShape: ImageVector
```

### AddSticker

```kotlin
val IconPack.AddSticker: ImageVector
```

### AddText

```kotlin
val IconPack.AddText: ImageVector
```

### Adjustments

```kotlin
val IconPack.Adjustments: ImageVector
```

### Animation

```kotlin
val IconPack.Animation: ImageVector
```

### ArrowBack

```kotlin
val IconPack.ArrowBack: ImageVector
```

### ArrowForward

```kotlin
val IconPack.ArrowForward: ImageVector
```

### Arrowback

```kotlin
val IconPack.Arrowback: ImageVector
```
> **Deprecated:** Use IconPack.ArrowBack instead. Replace with `IconPack.ArrowBack`.

### AsClip

```kotlin
val IconPack.AsClip: ImageVector
```

### AsOverlay

```kotlin
val IconPack.AsOverlay: ImageVector
```

### Blur

```kotlin
val IconPack.Blur: ImageVector
```

### BringForward

```kotlin
val IconPack.BringForward: ImageVector
```

### Close

```kotlin
val IconPack.Close: ImageVector
```

### CloudAlertOutline

```kotlin
val IconPack.CloudAlertOutline: ImageVector
```

### CropRotate

```kotlin
val IconPack.CropRotate: ImageVector
```

### Croprotate

```kotlin
val IconPack.Croprotate: ImageVector
```
> **Deprecated:** Use IconPack.CropRotate instead. Replace with `IconPack.CropRotate`.

### Delete

```kotlin
val IconPack.Delete: ImageVector
```

### Duplicate

```kotlin
val IconPack.Duplicate: ImageVector
```

### Effect

```kotlin
val IconPack.Effect: ImageVector
```

### Elements

```kotlin
val IconPack.Elements: ImageVector
```

### ExpandMore

```kotlin
val IconPack.ExpandMore: ImageVector
```

### Export

```kotlin
val IconPack.Export: ImageVector
```

### Filter

```kotlin
val IconPack.Filter: ImageVector
```

### GroupEnter

```kotlin
val IconPack.GroupEnter: ImageVector
```

### ImageOutline

```kotlin
val IconPack.ImageOutline: ImageVector
```

### Image

```kotlin
val IconPack.Image: ImageVector
```

### Imageoutline

```kotlin
val IconPack.Imageoutline: ImageVector
```
> **Deprecated:** Use IconPack.ImageOutline instead. Replace with `IconPack.ImageOutline`.

### Keyboard

```kotlin
val IconPack.Keyboard: ImageVector
```

### LayersOutline

```kotlin
val IconPack.LayersOutline: ImageVector
```

### LibraryElementsOutline

```kotlin
val IconPack.LibraryElementsOutline: ImageVector
```

### LibraryElements

```kotlin
val IconPack.LibraryElements: ImageVector
```

### Libraryelements

```kotlin
val IconPack.Libraryelements: ImageVector
```
> **Deprecated:** Use IconPack.LibraryElements instead. Replace with `IconPack.LibraryElements`.

### Libraryelementsoutline

```kotlin
val IconPack.Libraryelementsoutline: ImageVector
```
> **Deprecated:** Use IconPack.LibraryElementsOutline instead. Replace with `IconPack.LibraryElementsOutline`.

### Minus

```kotlin
val IconPack.Minus: ImageVector
```

### Music

```kotlin
val IconPack.Music: ImageVector
```

### Pages

```kotlin
val IconPack.Pages: ImageVector
```

### PlayBoxOutline

```kotlin
val IconPack.PlayBoxOutline: ImageVector
```

### PlayBox

```kotlin
val IconPack.PlayBox: ImageVector
```

### Playbox

```kotlin
val IconPack.Playbox: ImageVector
```
> **Deprecated:** Use IconPack.PlayBox instead. Replace with `IconPack.PlayBox`.

### Playboxoutline

```kotlin
val IconPack.Playboxoutline: ImageVector
```
> **Deprecated:** Use IconPack.PlayBoxOutline instead. Replace with `IconPack.PlayBoxOutline`.

### Plus

```kotlin
val IconPack.Plus: ImageVector
```

### PreviewToggled

```kotlin
val IconPack.PreviewToggled: ImageVector
```

### Preview

```kotlin
val IconPack.Preview: ImageVector
```

### Rabbit

```kotlin
val IconPack.Rabbit: ImageVector
```

### Redo

```kotlin
val IconPack.Redo: ImageVector
```

### ReorderHorizontally

```kotlin
val IconPack.ReorderHorizontally: ImageVector
```

### Reorderhorizontally

```kotlin
val IconPack.Reorderhorizontally: ImageVector
```
> **Deprecated:** Use IconPack.ReorderHorizontally instead. Replace with `IconPack.ReorderHorizontally`.

### Replace

```kotlin
val IconPack.Replace: ImageVector
```

### Resize

```kotlin
val IconPack.Resize: ImageVector
```

### SelectGroup

```kotlin
val IconPack.SelectGroup: ImageVector
```

### SendBackward

```kotlin
val IconPack.SendBackward: ImageVector
```

### ShapeIcon

```kotlin
val IconPack.ShapeIcon: ImageVector
```

### ShapesOutline

```kotlin
val IconPack.ShapesOutline: ImageVector
```

### Shapes

```kotlin
val IconPack.Shapes: ImageVector
```

### Shapesoutline

```kotlin
val IconPack.Shapesoutline: ImageVector
```
> **Deprecated:** Use IconPack.ShapesOutline instead. Replace with `IconPack.ShapesOutline`.

### SizeLCircled

```kotlin
val IconPack.SizeLCircled: ImageVector
```

### SizeL

```kotlin
val IconPack.SizeL: ImageVector
```

### SizeMCircled

```kotlin
val IconPack.SizeMCircled: ImageVector
```

### SizeM

```kotlin
val IconPack.SizeM: ImageVector
```

### SizeSCircled

```kotlin
val IconPack.SizeSCircled: ImageVector
```

### SizeS

```kotlin
val IconPack.SizeS: ImageVector
```

### Split

```kotlin
val IconPack.Split: ImageVector
```

### StickerEmojiOutline

```kotlin
val IconPack.StickerEmojiOutline: ImageVector
```

### StickerEmoji

```kotlin
val IconPack.StickerEmoji: ImageVector
```

### Stickeremoji

```kotlin
val IconPack.Stickeremoji: ImageVector
```
> **Deprecated:** Use IconPack.StickerEmoji instead. Replace with `IconPack.StickerEmoji`.

### Stickeremojioutline

```kotlin
val IconPack.Stickeremojioutline: ImageVector
```
> **Deprecated:** Use IconPack.StickerEmojiOutline instead. Replace with `IconPack.StickerEmojiOutline`.

### TextFields

```kotlin
val IconPack.TextFields: ImageVector
```

### TextOnPath

```kotlin
val IconPack.TextOnPath: ImageVector
```

### TextPresets

```kotlin
val IconPack.TextPresets: ImageVector
```

### Textfields

```kotlin
val IconPack.Textfields: ImageVector
```
> **Deprecated:** Use IconPack.TextFields instead. Replace with `IconPack.TextFields`.

### Typeface

```kotlin
val IconPack.Typeface: ImageVector
```

### Undo

```kotlin
val IconPack.Undo: ImageVector
```

### VoiceoverAdd

```kotlin
val IconPack.VoiceoverAdd: ImageVector
```

### VolumeHigh

```kotlin
val IconPack.VolumeHigh: ImageVector
```

### WifiCancel

```kotlin
val IconPack.WifiCancel: ImageVector
```
