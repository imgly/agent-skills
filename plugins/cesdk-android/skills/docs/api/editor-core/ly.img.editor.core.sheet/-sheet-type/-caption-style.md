# CaptionStyle

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.sheet`

A sheet that lists the caption style presets of the ly.img.caption.presets asset source. Applying one styles every caption on the track.

```kotlin
class CaptionStyle(val style: SheetStyle = SheetStyle()) : SheetType
```


## Members

### CaptionStyle

```kotlin
constructor(style: SheetStyle = SheetStyle())
```

### style

```kotlin
open override val style: SheetStyle
```
