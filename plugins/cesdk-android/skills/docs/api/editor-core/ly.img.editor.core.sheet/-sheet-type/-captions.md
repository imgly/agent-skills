# Captions

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.sheet`

A sheet that is used to add and edit captions in video scenes.

```kotlin
class Captions(val style: SheetStyle = SheetStyle( maxHeight = Height.Fraction(1F), )) : SheetType
```


## Members

### Captions

```kotlin
constructor(style: SheetStyle = SheetStyle( maxHeight = Height.Fraction(1F), ))
```

### style

```kotlin
open override val style: SheetStyle
```
