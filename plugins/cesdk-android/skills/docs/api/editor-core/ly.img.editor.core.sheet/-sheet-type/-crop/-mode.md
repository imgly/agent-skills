# Mode

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.sheet`

Configuration of the different crop and resize modes. Each flag enables or disables certain controls of the Crop sheet.

```kotlin
interface Mode
```


## Members

### applyOnAllPages

```kotlin
abstract val applyOnAllPages: Boolean
```

### hasContentFillMode

```kotlin
abstract val hasContentFillMode: Boolean
```

### hasCropAsset

```kotlin
abstract val hasCropAsset: Boolean
```

### hasPageAsset

```kotlin
abstract val hasPageAsset: Boolean
```

### hasResetButton

```kotlin
abstract val hasResetButton: Boolean
```

### hasResizeOption

```kotlin
abstract var hasResizeOption: Boolean
```

### hasRotateOptions

```kotlin
abstract val hasRotateOptions: Boolean
```

### titleRes

```kotlin
abstract val titleRes: Int
```
