# Effect

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.sheet`

A sheet that is used to set effects to design blocks with image and video fills.

```kotlin
class Effect(val style: SheetStyle = SheetStyle( minHeight = Height.Exactly(204.dp), )) : SheetType
```


## Members

### Effect

```kotlin
constructor(style: SheetStyle = SheetStyle( minHeight = Height.Exactly(204.dp), ))
```

### style

```kotlin
open override val style: SheetStyle
```
