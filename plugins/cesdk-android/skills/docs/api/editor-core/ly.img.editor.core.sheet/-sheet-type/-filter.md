# Filter

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.sheet`

A sheet that is used to set filters to design blocks with image and video fills.

```kotlin
class Filter(val style: SheetStyle = SheetStyle()) : SheetType
```


## Members

### Filter

```kotlin
constructor(style: SheetStyle = SheetStyle())
```

### style

```kotlin
open override val style: SheetStyle
```
