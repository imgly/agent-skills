# FontSize

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.sheet`

A sheet that is used to control the font size of the designBlock.

```kotlin
class FontSize(val style: SheetStyle = SheetStyle(), val designBlock: DesignBlock) : SheetType
```


## Members

### FontSize

```kotlin
constructor(style: SheetStyle = SheetStyle(), designBlock: DesignBlock)
```

### designBlock

```kotlin
val designBlock: DesignBlock
```

### style

```kotlin
open override val style: SheetStyle
```
