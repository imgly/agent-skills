# Voiceover

- **Module:** `ly.img:editor-core`
- **Package:** `ly.img.editor.core.sheet`

A sheet that is used to record new voice-over takes in video scenes.

```kotlin
class Voiceover(val style: SheetStyle = SheetStyle( isFloating = true, )) : SheetType
```


## Members

### Voiceover

```kotlin
constructor(style: SheetStyle = SheetStyle( isFloating = true, ))
```

### style

```kotlin
open override val style: SheetStyle
```
