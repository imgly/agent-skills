# Companion

- **Module:** `ly.img:editor`
- **Package:** `ly.img.editor`

```kotlin
object Companion
```


## Members

### KEY_STATE_ERROR

```kotlin
const val KEY_STATE_ERROR: String
```

The key to find the BasicConfigurationBuilder.error state property.

### KEY_STATE_SHOW_CONFIRMATION_DIALOG

```kotlin
const val KEY_STATE_SHOW_CONFIRMATION_DIALOG: String
```

The key to find the BasicConfigurationBuilder.showCloseConfirmationDialog state property.

### KEY_STATE_SHOW_LOADING

```kotlin
const val KEY_STATE_SHOW_LOADING: String
```

The key to find the BasicConfigurationBuilder.showLoading state property.

### KEY_STATE_SYSTEM_GALLERY_CONFIGURATION

```kotlin
const val KEY_STATE_SYSTEM_GALLERY_CONFIGURATION: String
```

The key to find the BasicConfigurationBuilder.systemGalleryConfiguration state property.
