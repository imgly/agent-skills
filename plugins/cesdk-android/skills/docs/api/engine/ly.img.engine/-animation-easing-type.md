# AnimationEasingType

- **Module:** `ly.img:engine`
- **Package:** `ly.img.engine`

```kotlin
enum AnimationEasingType : Enum<AnimationEasingType>
```


## Members

### EASE_IN

```kotlin
enum entry EASE_IN
```

### EASE_IN_BACK

```kotlin
enum entry EASE_IN_BACK
```

### EASE_IN_OUT

```kotlin
enum entry EASE_IN_OUT
```

### EASE_IN_OUT_BACK

```kotlin
enum entry EASE_IN_OUT_BACK
```

### EASE_IN_OUT_QUART

```kotlin
enum entry EASE_IN_OUT_QUART
```

### EASE_IN_OUT_QUINT

```kotlin
enum entry EASE_IN_OUT_QUINT
```

### EASE_IN_OUT_SPRING

```kotlin
enum entry EASE_IN_OUT_SPRING
```

### EASE_IN_QUART

```kotlin
enum entry EASE_IN_QUART
```

### EASE_IN_QUINT

```kotlin
enum entry EASE_IN_QUINT
```

### EASE_IN_SPRING

```kotlin
enum entry EASE_IN_SPRING
```

### EASE_OUT

```kotlin
enum entry EASE_OUT
```

### EASE_OUT_BACK

```kotlin
enum entry EASE_OUT_BACK
```

### EASE_OUT_QUART

```kotlin
enum entry EASE_OUT_QUART
```

### EASE_OUT_QUINT

```kotlin
enum entry EASE_OUT_QUINT
```

### EASE_OUT_SPRING

```kotlin
enum entry EASE_OUT_SPRING
```

### LINEAR

```kotlin
enum entry LINEAR
```

### entries

```kotlin
val entries: EnumEntries<AnimationEasingType>
```

Returns a representation of an immutable list of all enum entries, in the order they're declared. This method may be used to iterate over the enum entries.

### key

```kotlin
val key: String
```

### valueOf

```kotlin
fun valueOf(value: String): AnimationEasingType
```

Returns the enum constant of this type with the specified name. The string must match exactly an identifier used to declare an enum constant in this type. (Extraneous whitespace characters are not permitted.)

### values

```kotlin
fun values(): Array<AnimationEasingType>
```

Returns an array containing the constants of this enum type, in the order they're declared. This method may be used to iterate over the constants.
