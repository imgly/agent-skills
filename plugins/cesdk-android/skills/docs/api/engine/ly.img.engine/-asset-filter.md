# AssetFilter

- **Module:** `ly.img:engine`
- **Package:** `ly.img.engine`

A filter expression evaluated against a single asset. Property predicates (Equals, Contains) target a property path:

```kotlin
interface AssetFilter
```
