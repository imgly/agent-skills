# And

- **Module:** `ly.img:engine`
- **Package:** `ly.img.engine`

Matches if all filters match. Must not be empty.

```kotlin
data class And(val filters: List<AssetFilter>) : AssetFilter
```


## Members

### And

```kotlin
constructor(filters: List<AssetFilter>)
```

### filters

```kotlin
val filters: List<AssetFilter>
```
