# BlendMode

- **Module:** `ly.img:engine`
- **Package:** `ly.img.engine`

```kotlin
enum BlendMode : Enum<BlendMode>
```


## Members

### COLOR

```kotlin
enum entry COLOR
```

### COLOR_BURN

```kotlin
enum entry COLOR_BURN
```

### COLOR_DODGE

```kotlin
enum entry COLOR_DODGE
```

### DARKEN

```kotlin
enum entry DARKEN
```

### DARKEN_COLOR

```kotlin
enum entry DARKEN_COLOR
```

### DIFFERENCE

```kotlin
enum entry DIFFERENCE
```

### DIVIDE

```kotlin
enum entry DIVIDE
```

### EXCLUSION

```kotlin
enum entry EXCLUSION
```

### HARD_LIGHT

```kotlin
enum entry HARD_LIGHT
```

### HARD_MIX

```kotlin
enum entry HARD_MIX
```

### HUE

```kotlin
enum entry HUE
```

### LIGHTEN

```kotlin
enum entry LIGHTEN
```

### LIGHTEN_COLOR

```kotlin
enum entry LIGHTEN_COLOR
```

### LINEAR_BURN

```kotlin
enum entry LINEAR_BURN
```

### LINEAR_DODGE

```kotlin
enum entry LINEAR_DODGE
```

### LINEAR_LIGHT

```kotlin
enum entry LINEAR_LIGHT
```

### LUMINOSITY

```kotlin
enum entry LUMINOSITY
```

### MULTIPLY

```kotlin
enum entry MULTIPLY
```

### NORMAL

```kotlin
enum entry NORMAL
```

### OVERLAY

```kotlin
enum entry OVERLAY
```

### PASS_THROUGH

```kotlin
enum entry PASS_THROUGH
```

### PIN_LIGHT

```kotlin
enum entry PIN_LIGHT
```

### SATURATION

```kotlin
enum entry SATURATION
```

### SCREEN

```kotlin
enum entry SCREEN
```

### SOFT_LIGHT

```kotlin
enum entry SOFT_LIGHT
```

### SUBTRACT

```kotlin
enum entry SUBTRACT
```

### VIVID_LIGHT

```kotlin
enum entry VIVID_LIGHT
```

### entries

```kotlin
val entries: EnumEntries<BlendMode>
```

Returns a representation of an immutable list of all enum entries, in the order they're declared. This method may be used to iterate over the enum entries.

### valueOf

```kotlin
fun valueOf(value: String): BlendMode
```

Returns the enum constant of this type with the specified name. The string must match exactly an identifier used to declare an enum constant in this type. (Extraneous whitespace characters are not permitted.)

### value

```kotlin
val value: Int
```

### values

```kotlin
fun values(): Array<BlendMode>
```

Returns an array containing the constants of this enum type, in the order they're declared. This method may be used to iterate over the constants.
