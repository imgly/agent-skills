# SaveToArchiveOptions

- **Module:** `ly.img:engine`
- **Package:** `ly.img.engine`

Options for saving a scene to an archive.

```kotlin
data class SaveToArchiveOptions(val compression: CompressionOptions? = null)
```


## Members

### SaveToArchiveOptions

```kotlin
constructor(compression: CompressionOptions? = null)
```

### compression

```kotlin
val compression: CompressionOptions? = null
```
