# Foundation

- **Module:** `IMGLYCore`
- **DocC identifier:** `/documentation/IMGLYCore/Foundation`

## Members

### Locale.currentLanguageCode

```swift
static var currentLanguageCode: String { get }
```

The current device language code (e.g., `"en"`, `"de"`), falling back to `"en"` if unavailable.

### Locale

```swift
extension Locale
```
