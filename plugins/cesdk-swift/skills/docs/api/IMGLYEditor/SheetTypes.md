# SheetTypes

- **Module:** `IMGLYEditor`
- **DocC identifier:** `/documentation/IMGLYEditor/SheetTypes`

A namespace for [`SheetType`](sheettype.md)s.

```swift
enum SheetTypes
```

## Members

### FillStroke.fillOnly

```swift
let fillOnly: Bool
```

When `true`, only the fill section is shown (no stroke).

### SheetTypes.Adjustments

```swift
struct Adjustments
```

A sheet that is used to make adjustments to design blocks with image and video fills.

### SheetTypes.Animation

```swift
struct Animation
```

A sheet that is used to configure animations on design blocks.

### SheetTypes.Blur

```swift
struct Blur
```

A sheet that is used to set blurs to design blocks with image and video fills.

### SheetTypes.ClipSpeed

```swift
struct ClipSpeed
```

A sheet that is used to control playback speed of clips.

### SheetTypes.Crop

```swift
struct Crop
```

A sheet that is used to crop design blocks with image and video fills.

### SheetTypes.Effect

```swift
struct Effect
```

A sheet that is used to set effects to design blocks with image and video fills.

### SheetTypes.FillStroke

```swift
struct FillStroke
```

A sheet that is used to control the fill and/or stroke of various blocks.

### SheetTypes.Filter

```swift
struct Filter
```

A sheet that is used to set filters to design blocks with image and video fills.

### SheetTypes.FormatText

```swift
struct FormatText
```

A sheet that is used to control formatting of text blocks.

### SheetTypes.Layer

```swift
struct Layer
```

A sheet that is used to control the layering of design blocks.

### SheetTypes.LibraryAdd

```swift
struct LibraryAdd
```

A sheet to display the [`AssetLibrary`](../../imglycoreui/assetlibrary.md) in order to add assets to the scene..

### SheetTypes.LibraryReplace

```swift
struct LibraryReplace
```

A sheet to display the [`AssetLibrary`](../../imglycoreui/assetlibrary.md) in order to replace assets in the scene.

### SheetTypes.Reorder

```swift
struct Reorder
```

A sheet that is used to reorder videos on the background track.

### SheetTypes.Resize

```swift
struct Resize
```

A sheet that is used to resize pages.

### SheetTypes.Shape

```swift
struct Shape
```

A sheet that is used to control the shape of various blocks.

### SheetTypes.TextBackground

```swift
struct TextBackground
```

A sheet that is used to control the text background properties.

### SheetTypes.TextOnPath

```swift
struct TextOnPath
```

A sheet that is used to control placing text along an SVG baseline path.

### SheetTypes.Voiceover

```swift
struct Voiceover
```

A sheet that is used for voiceover recording.

### SheetTypes.Volume

```swift
struct Volume
```

A sheet that is used to control the volume of audio/video.

### Adjustments.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Animation.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Blur.style

```swift
let style: SheetStyle
```

The style of the sheet.

### ClipSpeed.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Crop.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Effect.style

```swift
let style: SheetStyle
```

The style of the sheet.

### FillStroke.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Filter.style

```swift
let style: SheetStyle
```

The style of the sheet.

### FormatText.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Layer.style

```swift
let style: SheetStyle
```

The style of the sheet.

### LibraryAdd.style

```swift
let style: SheetStyle
```

The style of the sheet.

### LibraryReplace.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Reorder.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Resize.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Shape.style

```swift
let style: SheetStyle
```

The style of the sheet.

### TextBackground.style

```swift
let style: SheetStyle
```

The style of the sheet.

### TextOnPath.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Voiceover.style

```swift
let style: SheetStyle
```

The style of the sheet.

### Volume.style

```swift
let style: SheetStyle
```

The style of the sheet.
