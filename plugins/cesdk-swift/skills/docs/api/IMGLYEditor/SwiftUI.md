# SwiftUI

- **Module:** `IMGLYEditor`
- **DocC identifier:** `/documentation/IMGLYEditor/SwiftUI`

## Members

### LabelStyle.imgly-31dyw

```swift
@MainActor static var imgly: IMGLY<AdaptiveIconOnlyLabelStyle>.Type { get }
```

Gets a namespace holder for `IMGLY` compatible types.

### LabelStyle.imgly-3d347

```swift
@MainActor static var imgly: IMGLY<CanvasMenuLabelStyle>.Type { get }
```

Gets a namespace holder for `IMGLY` compatible types.

### PrimitiveButtonStyle.imgly

```swift
@MainActor static var imgly: IMGLY<AssetLibraryButtonStyle>.Type { get }
```

Gets a namespace holder for `IMGLY` compatible types.

### LabelStyle

```swift
extension LabelStyle
```

### PrimitiveButtonStyle

```swift
extension PrimitiveButtonStyle
```
